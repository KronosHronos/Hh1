public abstract class Range extends BaseHero {
    public Range(int hp, String name, String type, int baseAttack, int initiative, int mp) {
        super(hp, name, type, baseAttack, initiative, mp);
    }
}